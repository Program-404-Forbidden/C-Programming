#include <stdio.h>
int main()
{
    int A, B;
    scanf("%d%d", &A, &B);
    if (A > B)
    {
        printf("%d is Greater", A);
    }
    else
    {
        printf("%d is Greater", B);
    }
    return 0;
}